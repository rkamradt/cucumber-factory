Given /^That I have no account$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I try to sign up$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I give my name, password \(x(\d+)\), company, email and role$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^the email is in a valid format and does not already exist in the system$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the two passwords match$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the company is a known vendoring organization$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the role is valid for a vendor account$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^an email should be sent to me to verify its validity which will include a link to add me to the system$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^an unconfirmed, unverified account will be created$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^an email will be sent to the company with a link to confirm me to the system$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^return instruction to me to check my email \(and navigation code for GUI interface\)$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the company is a known buying organization$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the role is valid for a client account$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I give my name, password \(x(\d+)\), supervisor, email and role$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^the supervisor is known$/ do
  pending # express the regexp above with the code you wish you had
end

When /^the role is valid for a internal user account$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^an email will be sent to my supervisor with a link to confirm me to the system$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^no other internal users exist$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I give my name, password \(x(\d+)\), and email$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^the email is in a valid format$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^an unverified account will be created$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^That I have an account$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^it is unconfirmed$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I try to sign in$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I give my name and password$/ do
  pending # express the regexp above with the code you wish you had
end

When /^they are correct$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^return that I am not yet confirmed and to check with my organization \(and navigation code for GUI interface\)$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^return that I am not yet confirmed and to check with my supervisor \(and navigation code for GUI interface\)$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^it is confirmed$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^return a key to the vendor system \(and navigation code for GUI interface\)$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^return a key to the client system \(and navigation code for GUI interface\)$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^return a key to the internal user system \(and navigation code for GUI interface\)$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^that an unverified or unconfirmed account exists$/ do
  pending # express the regexp above with the code you wish you had
end

Given /^five days have passed since the account was created$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^delete the account$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^send a message to the account email that they must re\-register$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^send a message to the organization\/supervisor that the user tried to create an account but it was unverified or unconfirmed$/ do
  pending # express the regexp above with the code you wish you had
end
